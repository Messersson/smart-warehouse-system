spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:warehouse_system}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:3709Lp}

wms:
  cors:
    allowed-origin-patterns: ${WMS_CORS_ALLOWED_ORIGINS:http://localhost,http://127.0.0.1}
